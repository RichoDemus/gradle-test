apply plugin: 'java'

repositories {
    //mavenLocal()
    mavenCentral()
}

dependencies {
    compile group:  'com.google.guava', name:'guava', version: '18.+'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}

